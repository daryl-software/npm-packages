name: Mocha CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
            
    steps:
    - uses: actions/checkout@v3
    - name: Node
      uses: actions/setup-node@v3
      with:
        node-version: 17
        cache: 'npm'
    - run: npm ci
    - run: npm run test
